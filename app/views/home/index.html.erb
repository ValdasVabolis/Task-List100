<h1>My Task List</h1>
<button id='add-new-task'>Add New Task</button>
<input type='text' name='Task Field' id='new-task'>
<table>
  <tr>
    <th>Task Description</th>
    <th>Complete</th>
  </tr>
  <% @tasks.each do |t| %>
    <tr>
      <td><%= t.description %></td>
      <td><%= t.complete? %></td>
    </tr>
  <% end %>
</table>
<% if @tasks.empty? %>
  It looks like your task list is empty! Add new tasks to get started.
<% end %>
